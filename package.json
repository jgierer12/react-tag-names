{
  "private": true,
  "scripts": {
    "build": "ts-node --compiler ttypescript build.ts",
    "test": "prettier --check --ignore-path .gitignore '**/*.{ts,json,md}'",
    "prerelease": "npm run build",
    "release": "yarn publish dist --non-interactive"
  },
  "dependencies": {
    "@types/react": "latest"
  },
  "devDependencies": {
    "@jgierer12/js-configs": "^1.0.1",
    "@types/mkdirp": "^0.5.2",
    "@types/node": "^12.0.10",
    "@types/rimraf": "^2.0.2",
    "mkdirp": "^0.5.1",
    "np": "^5.0.3",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "ts-node": "^8.3.0",
    "ts-transformer-keys": "^0.3.5",
    "ttypescript": "^1.5.7",
    "typescript": "^3.5.2"
  }
}
